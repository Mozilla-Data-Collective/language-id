name: glotlid
kind: standard
implementation: language_id.models.standard.glotlid_model:GlotLIDModel
# TODO: confirm canonical HF repo / weights at implementation time
hf_repo: cis-lmu/glotlid
version: TBD
options:
  top_k: 1

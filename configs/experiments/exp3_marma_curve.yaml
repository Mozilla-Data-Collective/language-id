experiment: exp3_marma_curve
description: OOD case study on Marma. Zero/few-shot LLM eval + data-efficiency curve for trained classifiers.
dataset: marma            # TODO: confirm loader identifier in `datacollective` (spec §16)

llm_eval:
  models:
    - gpt-5
    - claude-opus-4
    - qwen-3
    - mistral-large
  shots: [0, 1, 5]

data_efficiency:
  ns: [10, 50, 100, 500, 1000]
  models:
    - logreg
    - ngram_nb
    - xlmr
  seeds: [42, 43, 44]

wandb:
  project: lid-bench-exp3

[build-system]
requires = ["uv_build>=0.7.0,<0.10.0"]
build-backend = "uv_build"

[project]
name = "language-id"
version = "0.0.1"
description = "Benchmarking and building text language identification with CommonLID and CommonVoiceLID."
readme = "README.md"
requires-python = ">=3.12"
authors = [{ name = "Mozilla Data Collective" }]
license = { text = "MPL" }
keywords = ["language-identification", "lid", "nlp", "benchmark", "multilingual"]

dependencies = [
    # Core
    "datacollective>=0.5.1",
    "pandas>=2.2",
    "typer>=0.12",
    "langcodes>=3.4",
    "language-data>=1.4.0",
    # LLMs
    "together>=1.3",
    # "otari>=0.0.3",  # future migration
    # Standard LID tools
    "langdetect>=1.0.9",
    "fasttext-predict>=0.9.2",
    "huggingface-hub>=0.23",
    "matplotlib>=3.10.9",
    "tqdm>=4.66",
    # Train-your-own classifier (char n-gram logistic regression)
    "scikit-learn>=1.5",
    "jinja2>=3.1.6",
    "ipykernel>=7.2.0",
]

[project.optional-dependencies]
# Fine-tune an off-the-shelf HF LLM (e.g. Qwen) as a single-language detector.
finetune = [
    "torch>=2.2",
    "transformers>=4.51",
    "datasets>=2.19",
    "accelerate>=0.30",
    "ipywidgets>=8.1",  # renders the training progress bar inside notebooks
]
dev = [
    "ruff>=0.6",
    "ty>=0.0.1a1",
    "pre-commit>=3.7",
    "nbconvert>=7.17.1",
    "ipykernel>=7.2.0",
]
docs = [
  "mkdocs",
  "mkdocs-material",
  "mkdocstrings-python",
]

[project.scripts]
language-id = "language_id.cli:app"

[project.urls]
Homepage = "https://github.com/Mozilla-Data-Collective/language-id"
Documentation = "https://mozilla-data-collective.github.io/language-id/"

[tool.uv.build-backend]
module-name = "language_id"
module-root = "src"

[tool.ruff]
line-length = 100
src = ["src"]
extend-exclude = ["docs", "results"]

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "B",   # flake8-bugbear
    "UP",  # pyupgrade
    "SIM", # flake8-simplify
    "RUF",
]
ignore = [
    "E501",  # line length handled by formatter
]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.ty.src]
root = "src"
